{
	"dependencies": {
		"concurrently": "^3.5.1"
},
	"scripts": {
		"install": "(cd backend && node) && (cd frontend && yarn)",
		"start": "node backend/index.js",
		"heroku-postbuild": "cd backend && sequelize db:migrate && cd ../frontend/ && yarn install && yarn build"
	}
}
